# Core Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# CRITICAL: OpenTelemetry Instrumentation
# Why: Auto-instruments FastAPI, Redis, HTTP clients without manual span creation
# Trade-off: Adds ~5ms overhead per request, but saves 100+ hours of manual instrumentation
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-instrumentation-fastapi==0.43b0
opentelemetry-instrumentation-redis==0.43b0
opentelemetry-instrumentation-requests==0.43b0
opentelemetry-instrumentation-logging==0.43b0
opentelemetry-exporter-otlp==1.22.0
opentelemetry-exporter-prometheus==0.43b0

# Prometheus Client for Custom Metrics
# Why: Built-in counters/histograms/gauges for business metrics
# When to NOT use: If you only need auto-instrumented metrics (OTel covers 80%)
prometheus-client==0.19.0

# ML Stack
scikit-learn==1.4.0      # For demo fraud model
numpy==1.26.3
pandas==2.1.4
joblib==1.3.2            # Model serialization

# Data Stores
redis==5.0.1             # Feature store (in-memory cache)
kafka-python==2.0.2      # Event streaming

# Structured Logging
# CRITICAL: JSON logs are parseable by Loki/ELK. Text logs require regex parsing (brittle)
python-json-logger==2.0.7
structlog==24.1.0

# HTTP Client
httpx==0.26.0            # Async HTTP (better than requests for async FastAPI)

# Testing & Chaos Engineering
locust==2.20.0           # Load testing
pytest==7.4.4
pytest-asyncio==0.23.3

# Utilities
python-dotenv==1.0.0
